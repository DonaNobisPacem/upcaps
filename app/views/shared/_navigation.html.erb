<% if user_signed_in? %>
	<nav class="navbar navbar-default navbar-fixed-top">
		<div class="container-fluid">
		    <div class="navbar-header">
		     	<%= link_to root_path, :class=> "navbar-brand" do %>
				  	<i class="fa fa-home"></i> Home
				<% end %>
		    </div>
		    <div class="collapse navbar-collapse">
		    	<ul class="nav navbar-nav">
			     	<li class="dropdown">
			     		<%= link_to 'Campus <i class="fa fa-caret-down"></i>'.html_safe, campus_path, :id => 'campusdrop', :'data-toggle' => 'dropdown', :class => 'dropdown-toggle' %>
			     		<ul class="dropdown-menu">
							<li>
								<%= link_to campus_path do %>
								  	<i class="fa fa-plus"></i> View Campus
								<% end %>
							</li>
							<li>
								<%= link_to new_campu_path do %>
								<i class="fa fa-plus"></i> Add Campus
								<% end %>
							</li>
						</ul>
					</li>
				</ul>
	      		<ul class="nav navbar-nav navbar-right">
			      	<li class="">
						<%= link_to(destroy_user_session_path, :method=>'delete') do %>
						<i class="fa fa-sign-out"></i> Logout
						<% end %>
					</li>
		      	</ul>
		    </div>
	  	</div>
	</nav>
<% else %>
</br></br>
<% end %>